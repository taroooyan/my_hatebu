<%# @entries = {:title, :link, :description, :creator, :date, :tag, :head_text} %>
<div class="row">
    <% @entries.each do |entry| %>
      <div class="col-sm-3">
      <ul>
        <li><%= link_to entry[:title], entry[:link], :target=>["_blank"] %></li>
        <li><%= entry[:head_text] %></li>
        <li><%= entry[:creator] %></li>
        <li><%= entry[:description] %></li>
        <li><%= entry[:date] %></li>
      </ul>
    </div>
    <% end %>
</div>
